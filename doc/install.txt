# Clonar el repositorio
git clone <user>@varpa.it.uc3m.es:~jaf/lib/www/camgrade/git

# Installar paquetes requeridos
sudo aptitude install python-pygame
sudo aptitude install python-opencv
sudo aptitude install libcv4
sudo aptitude install libavformat52


# Fichero de configuraci??n
# camera-dev: 0 si es la primera c??mara
# si el ordenador tiene ya una integrada, camera-dev: 1
cp <camgrade-DIR>/git/doc/camgrade.cfg.sample ~/.camgrade.cfg

cd <camgrade-DIR>/git/src
python camgrade.py

